---
const WORK = [
	{
		title: "Senior Software Engineer",
		period: "Dec 2024 — Now",
		company: "Guardian Life",
		url: "https://www.guardianlife.com",
		before:
			"Reworked the main dashboard's render path. Median load dropped from ",
		metric: "2.4s to 0.9s",
		after: " and weekly active use rose 18%.",
	},
];
---

<section id="work" class="section-wrapper" aria-labelledby="work-label">
	<h2 id="work-label" class="section-label">
		<span class="text-(--accent)">02</span> &nbsp;— &nbsp;Work
	</h2>

	{
		WORK.map((role) => (
			<div class="border-t border-(--hairline) py-6">
				<div class="mb-1.5 title-row">
					<h3 class="text-[21px] font-medium tracking-[-0.01em] text-(--ink)">
						{role.title}
					</h3>
					<span class="whitespace-nowrap text-[12px] text-(--faint)">
						{role.period}
					</span>
				</div>
				<div class="mb-3.5 text-[15px] text-(--muted)">
					<a
						href={role.url}
						target="_blank"
						rel="noopener noreferrer"
						aria-label={`${role.company} opens in a new tab`}
						class="link-soft text-[15px]"
					>
						{role.company}<span class="ml-1 inline-block no-underline" aria-hidden="true">↗</span>
					</a>
				</div>
				<p class="max-w-[62ch] text-pretty text-[16px] leading-[1.6] text-(--ink-soft)">
					{role.before}
					<span class="font-medium text-(--accent)">
						{role.metric}
					</span>
					{role.after}
				</p>
			</div>
		))
	}
</section>
